node_modules/
dist/
.output/
.vinxi/
.tanstack/
.nitro/
.data/
# Keep edge persistence local on servers (not in git)
!.data/.gitkeep
.env
.env.*
!.env.example
.deploy/
screenshots/
*.log
/tmp/
.DS_Store
.idea/
.project_id
.github_repo

# Android local build artifacts
android/.gradle/
android/**/build/
android/local.properties
android/.cxx/
*.apk
*.aab
*.ap_
# Tracked Play upload AABs (manual Console upload)
!google-play/releases/
!google-play/releases/**/*.aab
*.dex

# App Store / TestFlight binaries (docs under apple-store/ are tracked)
*.ipa
!apple-store/releases/
!apple-store/releases/**/*.ipa

# Fastlane / Play Console secrets (never commit)
android/secrets/
android/fastlane/report.xml
android/fastlane/README.md
ios/secrets/**
!ios/secrets/
!ios/secrets/.gitkeep
!ios/secrets/**/
!ios/secrets/**/*.example
ios/fastlane/report.xml
ios/build/
**/play-store-service-account.json
*.jks
*.keystore
android/key.properties

# Local play demo captures (large; optional)
artifacts/play-demo/

# Local secrets (service accounts, keystores) — never commit
android/secrets/
**/play-store-service-account.json
**/busyproxy-play-api-service-account.json
*.jks
*.keystore
android/key.properties
