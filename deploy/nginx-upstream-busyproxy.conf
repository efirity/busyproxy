# Active Node slot for BusyProxy (blue/green).
# Managed by scripts/zero-downtime-restart.sh — do not hard-code ports in site config.
# Active port is also recorded in /var/lib/busyproxy/active_port

upstream busyproxy_app {
  server 127.0.0.1:8080;
  keepalive 32;
}
